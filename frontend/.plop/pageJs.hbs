import React from 'react';
import styles from './{{name}}.module.scss';

export default function {{name}}({content, children}) {
  return(
    <div className={`${styles.page} {{kebabCase name}}-page`}>
      {children}
    </div>
  );
}

